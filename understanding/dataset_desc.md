## TS datasets
- **ts_entities**: time series with occurrences for words (split into ts_entities_split)
- **ts_tokens**: time series with occurrences for tokens (split into ts_tokens_split)


## release
- **year_spacy**: spacy file, saved in vertical format. 1 per year (split into year_spacy_split)
- **year_clean**: (i think) same as year_spacy but with og text (yet to be split)


## dict_all
- **entity_all**:
- **lemma_all**:
- **token_all**: 